[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "coreason_etl_purple_book"
dynamic = ["version"]
description = "ETL pipeline for the FDA Purple Book of biological products"
readme = "README.md"
requires-python = ">=3.14"
authors = [
    { name = "Akshaya M", email = "akshaya.movvar@coreason.ai" },
]
dependencies = [
    "loguru>=0.7.2",
    "pydantic>=2.12.5",
    "dlt>=1.0.0",
    "polars>=1.39.0",
    "pyarrow>=23.0.1",
    "psycopg2-binary>=2.9.11",
    "connectorx>=0.4.5; sys_platform == 'win32' or (sys_platform == 'linux' and platform_machine == 'x86_64' and python_version < '3.14t')",
    "adbc-driver-postgresql>=1.10.0",
]
license = { file = "LICENSE" }
classifiers = [
    "License :: Other/Proprietary License",
    "Operating System :: OS Independent",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.14.14",
    "pre-commit>=4.5.1",
    "pytest-cov>=7.0.0",
    "zensical",
    "mypy>=1.19.1",
    "pytest-asyncio",
    "pytest-benchmark",
    "deptry",
    "deepdiff",
    "hypothesis",
    "syrupy",
    "mkdocstrings-python",
    "types-pyyaml>=6.0.12.20250915",
    "types-requests>=2.32.4.20260107",
    "tzdata>=2025.3",
    "pytest-xdist>=3.6.1",
    "pytest-mock>=3.15.1",
    "httpx>=0.28.1",
    "pytest-httpx>=0.36.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/coreason_etl_purple_book"]

[tool.hatch.version]
source = "vcs"

[project.urls]
Homepage = "https://github.com/CoReason-AI/coreason_etl_purple_book"
Repository = "https://github.com/CoReason-AI/coreason_etl_purple_book"
Documentation = "https://github.com/CoReason-AI/coreason_etl_purple_book"

[tool.ruff]
line-length = 120
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "B", "I", "UP", "SIM", "RUF", "ARG", "C4", "PT", "TCH", "FA", "PIE", "RET", "PERF", "FURB", "LOG", "N", "A", "S"]
ignore = ["S101", "TC001", "TC002", "TC003", "UP037"]

[tool.mypy]
python_version = "3.14"
strict = true
disallow_untyped_defs = true
warn_unused_ignores = true
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
addopts = "-n auto --cov=src --cov-report=term-missing --cov-report=html --cov-fail-under=100"
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.coverage.run]
omit = ["tests/*"]
